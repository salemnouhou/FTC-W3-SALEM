"use client";
const Hero: React.FC = () => {
 
  return (
    <section  className="relative w-full bg-secondary-500 overflow-hidden flex flex-col">
     
    </section>
  );
};

export default Hero;

